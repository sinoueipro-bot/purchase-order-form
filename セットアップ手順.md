# 発注書フォーム セットアップ手順

## ファイル構成
```
purchase-order-gas/
  Code.gs     ← GASに貼り付けるスクリプト
  index.html  ← SPで開くフォーム画面
```

## セットアップ（10分程度）

### Step 1: Googleスプレッドシートを作成
1. Google Driveで「新規 > Googleスプレッドシート」
2. ファイル名を「発注書データ」に変更

### Step 2: Apps Scriptにコードを貼り付け
1. スプレッドシートのメニュー「拡張機能 > Apps Script」を開く
2. デフォルトの`コード.gs`の内容を全て削除
3. `Code.gs`の内容を全て貼り付け
4. 保存（Ctrl+S）

### Step 3: ヘッダー行を作成
1. Apps Scriptの上部で関数選択を「initSheet」に変更
2. 「実行」ボタンをクリック
3. 初回は「承認が必要です」→「許可を確認」→ Googleアカウントでログイン → 「許可」
4. スプレッドシートに「発注データ」シートとヘッダー行が作成される

### Step 4: WebアプリとしてデプロイStep 4: Webアプリとしてデプロイ
1. Apps Scriptの右上「デプロイ > 新しいデプロイ」
2. 種類の選択で「ウェブアプリ」を選択
3. 設定:
   - 説明: 発注書API
   - 次のユーザーとして実行: **自分**
   - アクセスできるユーザー: **全員**
4. 「デプロイ」をクリック
5. 表示される**ウェブアプリURL**をコピー（※これが重要！）

### Step 5: HTMLにURLを設定
1. `index.html`をテキストエディタで開く
2. 以下の行を見つける:
   ```
   const GAS_URL = 'YOUR_GAS_WEB_APP_URL_HERE';
   ```
3. `YOUR_GAS_WEB_APP_URL_HERE` をStep 4でコピーしたURLに置き換える
4. 保存

### Step 6: 承認者のメールアドレスを設定
1. `index.html`の`APPROVERS`配列を編集
2. `email: 'harada@example.com'` を実際のメールアドレスに変更
3. 保存

## 使い方
1. `index.html`をスマホのブラウザで開く（LINEやメールで送付 or WEB公開）
2. 必要事項を入力
3. 「送信」ボタン → プレビュー確認 → 「この内容で送信する」
4. 自動的に:
   - Googleスプレッドシートに1行追記
   - 承認者にHTML整形メールが届く（スプシリンク付き）

## GAS未設定でも使える
GAS_URLが未設定の場合、フォールバックとしてmailto:でメーラーが起動します。
まずフォームの使い勝手を確認してからGAS連携を設定することも可能です。

## トラブルシューティング
- **送信エラー**: GAS URLが正しいか確認。デプロイ後にURLが変わることがある
- **メールが届かない**: GASの実行ログを確認（Apps Script > 実行数）
- **スプレッドシートに書き込まれない**: initSheet()を実行したか確認
- **権限エラー**: デプロイ設定で「アクセスできるユーザー: 全員」になっているか確認

## 統合ダッシュボード（LINE / LINE WORKS / Chatwork / Gmail / 広告）

2026-08-29追加。1画面ダッシュボードとLINE系の受信蓄積のセットアップは
`docs/ダッシュボード_LINE_LINEWORKS連携.md` を参照。
